"""Backward-compatible re-exports for legacy import paths.

Canonical implementations live in:
  parse_documents/   — document parsing
  generate_outline/  — LLM TO generation
  finalize_output/   — post-processing
"""
